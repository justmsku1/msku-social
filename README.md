Security Note: The Firebase API Key visible in the configuration is intentionally exposed but secured via strict domain restrictions (HTTP Referrers). It can only be used from authorized domains.
## 🔒 Security Note
The Firebase API Key visible in the source code is intentionally exposed. 
It is secured via **HTTP Referrer restrictions** in Google Cloud Console 
and can only be used from authorized domains (justmsku.firebaseapp.com & github.io).
